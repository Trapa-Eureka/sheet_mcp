// 사람 전용 수동 스모크 스크립트. 실제 시트/이메일을 사용한다 — CI/에이전트 게이트에 포함하지 않는다.
// 현재는 T0 스캐폴딩 단계 — 실제 구현은 T10에서 한다 (docs/TASKS.md T10, docs/TESTING.md §3).

function main(): void {
  console.log("sheet-mcp smoke: 아직 구현되지 않았습니다 (T10 참고).");
}

main();
